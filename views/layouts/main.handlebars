<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DrawingTube</title>
    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        #cnv {
            top: 0;
            left: 0;
            position: absolute;
        }

        #console {
            display: flex;
            padding: 5px;

            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            z-index: 10;
        }

        #canvasConsole {
            display: flex;
            padding: 5px;

            position: absolute;
            top: 100%;
            left: 50%;
            transform: translate(-50%, -100%);
            z-index: 10;
        }

        .standout {
            z-index: 5;
        }

        #player {
            position: absolute;
            z-index: 2;
        }

        .joinLink {
            border-radius: 10px;
            padding: 10px;
            background-color: #123;
            color: white;
            text-decoration: none;
        }

        .joinLink:hover {
            background-color: #234;
        }
    </style>
</head>

<body>
    {{{body}}}
</body>

</html>